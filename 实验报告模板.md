# 稳定可靠的棉花颜色识别实验文档

## 1. 课题目标

本实验面向未漂白原棉图像，设计一套**稳定、鲁棒、可复现**的颜色识别算法。任务要求对每张图像输出：

1. 棉花区域的主颜色（CIELAB Lab）；
2. 在标准颜色库中的 Top-3 最相近颜色名称及其 $\Delta E_{00}$；
3. 同一类别多张图像之间的识别一致性（主色两两间最大 $\Delta E_{00}$）。

题目要求 6 张图像结果具有较高一致性，理想情况下类内最大色差应尽量小，并满足题设给出的稳定性约束。

---

## 2. 数据组织与实际适配

### 2.1 题目描述

题目说明中给出了两部分关键信息：

- 每种棉花颜色共 10 张图像；
- 可参考 `dataLoader.py`，按每类 **6 张训练 / 4 张测试** 的方式划分；
- 需要输出每张图的主色和颜色库匹配结果，并统计一致性。

### 2.2 当前实际目录

本次实际目录结构如下：

```text
cotton_image/
├── colorful/
│   ├── 10
│   ├── 11
│   ├── 12
│   ├── 13
│   ├── 14
│   ├── 15
│   ├── 16
│   ├── 7
│   ├── 8
│   └── 9
└── gray/
    ├── 1
    ├── 2
    ├── 3
    ├── 4
    ├── 5
    └── 6
```

其中：

- `gray/1~6` 表示灰系棉花类别；
- `colorful/7~16` 表示彩色棉花类别；
- 每个子文件夹对应一种棉花颜色类别。

### 2.3 颜色库现状

题目文字中说明颜色库包含 `name, hex, rgb, lab_D65` 字段，但当前上传的 `color_dataset(1).json` 实际以 `rgb/hex` 为主，因此本方案在实现时加入了**自动 RGB→Lab 转换**，避免因字段不一致导致程序无法运行。

---

## 3. 为什么本任务不适合直接做“深度学习分类”

本题每类只有 10 张图像，而且任务核心不是“类别编号分类”，而是：

- 从图像中提取**稳定的物理颜色表示**；
- 与标准颜色库做**颜色距离匹配**；
- 保证不同蓬松度、面积和轻微阴影变化下结果一致。

因此，若直接训练 CNN 分类器，会有几个问题：

1. **样本极少**，极易过拟合；
2. 题目最终需要的是 **Lab 主色与颜色名匹配**，不是单纯类别标签；
3. 光照和蓬松度变化更适合用**颜色校正 + 前景分割 + 鲁棒统计**解决；
4. 无监督颜色测量方案更容易解释，也更符合“稳定可靠”的要求。

所以本实验采用的是**传统视觉 + 色彩空间分析**方案，而不是神经网络分类方案。

---

## 4. 总体方法设计

本实验的完整流程如下：

1. **读取图像**
2. **估计白纸背景颜色**
3. **基于背景进行白平衡校正**
4. **分割棉花前景区域**
5. **提取前景区域的鲁棒主色 Lab**
6. **与颜色库逐一计算 $\Delta E_{00}$**
7. **输出 Top-3 最近颜色**
8. **统计同类多图的一致性**

整体思路可以概括为：

> 先尽量消除背景和轻微光照影响，再只在棉花主体区域上计算主色，并用鲁棒统计抑制高光、阴影和局部松散结构造成的偏差。

---

## 5. 关键算法设计

## 5.1 白纸背景估计

由于拍摄条件中说明：

- 背景为统一白色纸板；
- 光源、角度基本一致；

因此可以把图像边缘区域视为背景的高概率区域。代码中从图像四周提取边框像素，再挑选：

- 亮度较高；
- RGB 三通道差异较小；

的像素作为背景候选，最后用中位数估计背景白色值。

这样做的好处是：

- 对小面积棉花仍然有效；
- 对局部阴影不敏感；
- 比直接假定 `[255,255,255]` 更符合真实拍摄条件。

---

## 5.2 白平衡校正

不同照片即使在同一环境下，也会有轻微曝光和色偏差异。  
因此，先将背景估计值缩放到一个固定参考白（代码里默认约 245），对 RGB 三通道分别进行缩放，从而把各张图像的背景尽量拉回同一白参考。

作用：

- 降低不同照片之间的亮度差异；
- 降低背景偏黄、偏蓝等微小色偏；
- 为后续的 Lab 计算提供更统一的输入。

---

## 5.3 棉花前景分割

仅在整张图上取平均颜色会被白背景强烈干扰，所以必须先分割出棉花区域。

本方案采用的不是单纯 RGB 阈值，而是：

1. 把图像转为 **Lab 色彩空间**；
2. 估计背景 Lab；
3. 计算每个像素与背景 Lab 的距离；
4. 使用**自适应阈值**区分前景与背景；
5. 再通过形态学操作清理掩膜。

之所以选 Lab，是因为：

- Lab 更接近人眼感知；
- 在 Lab 中测颜色差异更合理；
- 后续匹配颜色库本来也要用 Lab / $\Delta E_{00}$。

形态学处理中使用了：

- 开运算：去掉零散噪点；
- 闭运算：补齐棉花区域中的空洞；
- 孔洞填充；
- 保留最大连通域。

这样可以有效防止背景残留或前景碎裂。

---

## 5.4 鲁棒主色提取

棉花是蓬松物体，表面会出现：

- 高光区域；
- 阴影区域；
- 局部褶皱；
- 采样面积差异带来的边缘影响。

如果直接对所有前景像素取平均，主色会被这些极端像素拉偏。  
因此，代码中采用了**鲁棒主色估计**：

1. 仅保留前景像素；
2. 对亮度 $L$ 去掉两端极值（如最亮和最暗的部分）；
3. 对剩余像素：
   - $L$ 使用截尾均值；
   - $a,b$ 使用中位数。

这样做的原因是：

- $L$ 受阴影和高光影响最大，截尾均值更稳；
- $a,b$ 更能反映综合色调，中位数对少量异常像素更鲁棒。

最终得到的 Lab 更稳定，更适合满足“6 张图高度一致”的要求。

---

## 5.5 颜色匹配：使用 $\Delta E_{00}$

颜色库匹配不是比较 RGB 欧氏距离，而是使用 **CIEDE2000 ($\Delta E_{00}$)**。  
原因如下：

1. $\Delta E_{00}$ 是更接近人眼感知的色差度量；
2. 对亮度、色调、色度差异的建模更合理；
3. 比简单的 RGB 距离或 Lab 欧氏距离更适合颜色检索任务。

对每张图像提取到的主色 Lab，与颜色库中每个颜色的 Lab 逐一计算 $\Delta E_{00}$，再取最小的前三项作为 Top-3 输出。

---

## 6. 一致性指标设计

题目强调 6 张图像的结果要高度一致。  
因此，本实验定义同一类别的**一致性分数**为：

> 同类图像主色两两之间的最大 $\Delta E_{00}$

即：

- 先得到同类若干张图像的主色 Lab；
- 两两计算 $\Delta E_{00}$；
- 取其中最大值作为 `consistency_score`。

这个定义与题目示例中的“6 张图主色两两间最大 $\Delta E_{00}$”一致，能够直接反映最坏情况下的波动大小。

数值越小，说明该类别图像的识别越稳定。

---

## 7. 训练/测试划分的含义

虽然本方案本质是无监督颜色测量，并不“训练模型参数”，但仍然保留 **6 张 train / 4 张 test** 的划分，意义在于：

- `train`：用于观察同一类别在主要样本上的颜色一致性；
- `test`：用于验证同一方法对未参与“原型统计”的剩余图像是否仍然稳定。

这里的“训练集原型色”定义为：

- 该类别 6 张 train 图像主色 Lab 的中位数原型。

而“测试集原型色”定义为：

- 该类别 4 张 test 图像主色 Lab 的中位数原型。

再计算 train 原型与 test 原型之间的 $\Delta E_{00}$，可以评价该类棉花在未见图像上的泛化稳定性。

---

## 8. 代码实现说明

提交的主程序为：

```text
cotton_color_recognition.py
```

程序主要模块如下：

### 8.1 色彩空间模块

包含：

- `rgb_to_lab`
- `xyz_to_lab`
- `delta_e_ciede2000`

功能：

- 将图像 RGB 转换为 Lab；
- 计算颜色库匹配所需的 $\Delta E_{00}$。

### 8.2 数据集遍历与划分模块

包含：

- `discover_classes`
- `split_image_paths`

功能：

- 自动识别 `gray` 与 `colorful` 两种数据子集；
- 自动遍历每个类别文件夹；
- 按固定随机种子进行 6/4 划分。

### 8.3 图像处理模块

包含：

- `estimate_background_rgb`
- `white_balance_with_background`
- `build_cotton_mask`
- `robust_dominant_lab`

功能：

- 估计背景白；
- 进行白平衡；
- 分割棉花区域；
- 计算鲁棒主色。

### 8.4 汇总输出模块

包含：

- `process_single_image`
- `summarize_split`
- `process_dataset`

功能：

- 对每张图输出主色与 Top-3；
- 对每个类别输出一致性和原型色；
- 对整个数据集输出平均一致性与最大一致性。

---

## 9. 运行方式

假设你的目录为：

```text
~/C/p/c/cotton_image
```

颜色库为：

```text
./color_dataset(1).json
```

运行命令示例：

```bash
python cotton_color_recognition.py \
  --dataset_root ~/C/p/c/cotton_image \
  --color_db ./color_dataset(1).json \
  --output_json cotton_color_results.json
```

运行后会得到：

```text
cotton_color_results.json
```

其中包含：

- 每张图像的主色 Lab；
- 该主色在颜色库中的 Top-3 匹配；
- 每个类别 train/test 的一致性；
- 整体数据集的平均一致性与最大一致性。

---

## 10. 输出结果结构说明

输出 JSON 的结构大致如下：

```json
{
  "meta": {
    "dataset_root": "...",
    "color_db_path": "...",
    "seed": 42,
    "train_n": 6,
    "test_n": 4,
    "color_db_size": 2625
  },
  "datasets": {
    "gray": {
      "1": {
        "train": {
          "images": [...],
          "prototype_lab": [.., .., ..],
          "prototype_top3": [...],
          "consistency_score": 1.23
        },
        "test": {
          "images": [...],
          "prototype_lab": [.., .., ..],
          "prototype_top3": [...],
          "consistency_score": 1.51
        },
        "prototype_gap_train_vs_test": 0.94
      }
    }
  },
  "overall": {
    "mean_train_consistency": ...,
    "mean_test_consistency": ...,
    "max_train_consistency": ...,
    "max_test_consistency": ...
  }
}
```

---

## 11. 预期实验现象与分析思路

由于当前对话中没有实际上传棉花图片，因此这里不填写伪造数值，只说明运行后应如何分析。

### 11.1 关注的核心指标

1. **单图匹配准确性**
   - 主色是否落在正确色域；
   - Top-3 中是否出现语义合理的颜色名称。

2. **类内一致性**
   - 同一类别 6 张 train 图像的一致性是否足够小；
   - 4 张 test 图像的一致性是否仍然稳定。

3. **train/test 原型差异**
   - 若 `prototype_gap_train_vs_test` 很小，说明泛化稳定；
   - 若差异偏大，需检查分割是否受面积或阴影影响。

### 11.2 理想现象

理想情况下应看到：

- 大部分类别的 `consistency_score` 明显小于题目阈值；
- gray 组整体波动较小；
- colorful 组由于饱和度更高，Top-3 名称往往更稳定；
- train/test 原型差异整体较小。

### 11.3 若结果不稳定，应优先检查什么

若某些类别的一致性偏差较大，优先检查：

1. 该类图像中棉花是否贴近边框，导致背景估计错误；
2. 前景分割面积是否过小或过大；
3. 某张图是否出现明显高光或阴影；
4. 该类颜色是否过浅，接近白背景，从而增加分割难度。

---

## 12. 本方案的优点

本实验方案具有以下优点：

### 12.1 不依赖大规模训练

在每类仅 10 张图的前提下，无监督方案比深度学习分类器更稳妥。

### 12.2 可解释性强

每一步都可以清晰说明其作用：

- 白平衡为什么做；
- 分割为什么这样设；
- 主色为什么用鲁棒统计；
- 匹配为什么选 $\Delta E_{00}$。

### 12.3 与题目要求高度对齐

题目最终需要的是：

- 主色 Lab；
- 标准颜色匹配；
- 多图一致性。

本方案直接围绕这三点构建，避免了无关的模型复杂度。

### 12.4 对轻微形态变化更鲁棒

通过：

- 背景归一化；
- 前景分割；
- 去高光/去阴影；
- 鲁棒主色统计；

能够较好抑制“疏松 vs 紧实”“小团 vs 大簇”等因素引起的颜色波动。

---

## 13. 可能的改进方向

如果后续你确实拿到全部图片并运行后发现某些类别稳定性仍不足，可以继续做这些增强：

### 13.1 增加背景建模约束

例如只使用四角区域估计背景，而不是整圈边框。

### 13.2 改进主色估计

可以尝试：

- Lab 空间 K-Means；
- 以前景像素的色彩密度峰值作为主色；
- 或者计算 Lab medoid（最小总距离点）。

### 13.3 引入颜色校正板思想

若后续拍摄条件可控，可加入标准白参考区域，从而进一步提高跨图一致性。

### 13.4 保存中间可视化结果

例如输出：

- 白平衡后的图；
- 前景掩膜；
- 前景像素 Lab 分布；

这样更有利于写实验报告和排查异常样本。

---

## 14. 结论

本实验针对棉花颜色识别这一**小样本、强稳定性要求**任务，设计了一套基于传统图像处理和感知色差度量的解决方案。  
该方案不依赖复杂模型训练，而是通过：

- 背景白估计与白平衡，
- Lab 空间前景分割，
- 鲁棒主色提取，
- 基于 $\Delta E_{00}$ 的颜色库匹配，

实现对棉花主颜色的稳定识别，并通过类内最大 $\Delta E_{00}$ 来量化多图一致性。  
从任务目标、样本规模和可解释性角度看，这一方案比直接训练分类网络更合适，也更容易满足“稳定可靠”的要求。

---

## 15. 附：建议提交文件

建议最终提交以下文件：

```text
cotton_color_recognition.py     # 主程序
cotton_color_results.json       # 运行结果
experiment_report.md            # 实验文档
```

如果老师允许，还可以补充：

```text
debug_masks/                    # 前景掩膜可视化
debug_balanced/                 # 白平衡结果图
```

这样会更完整，也更利于展示你的方法设计过程。