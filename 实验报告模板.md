# 稳定可靠的棉花颜色识别系统实验报告

## 1. 课题背景与目标

本课题要求对未漂白原棉图像进行自动主颜色识别。数据集中包含 16 种不同颜色的棉花，每种颜色具有 10 张在统一白色背景、统一光照和统一拍摄角度下采集的图像，但仍存在蓬松度差异、取样面积差异以及局部阴影差异。因此，本课题不仅要求识别主颜色，还要求同一颜色棉花在多张图像中的识别结果保持高度一致。

本实验的具体目标如下：

1. 对每张棉花图像自动提取主颜色的 CIELAB Lab 值；
2. 在标准颜色数据库 `color_dataset.json` 中找到最接近的 Top-3 颜色名称及其 ΔE00 色差；
3. 保证同一颜色棉花多张图像之间的识别结果稳定一致，即组内最大 ΔE00 尽可能小；
4. 设计一套具有鲁棒性、可解释性和可复现性的算法流程。

---

## 2. 数据集与任务分析

实验数据由两部分组成：

- **棉花图像数据集**：共 16 种颜色，每种颜色 10 张图片；
- **标准颜色数据库**：共 217 种颜色，每种颜色含 `name`、`hex`、`rgb`、`lab_D65` 字段。

从任务特点来看，本课题更接近“工业颜色测量”而不是“复杂视觉识别”。由于背景为统一白色纸板、光照与角度相对固定，因此直接采用颜色科学方法进行建模更具优势。相比训练深度神经网络，使用 Lab 空间与色差公式进行稳健估计，能够获得更好的解释性和更高的一致性。

---

## 3. 算法总体设计

本文采用“**背景校正 + 前景分割 + 稳健主色提取 + 多图原型稳定化 + 数据库匹配**”的总体流程。

### 3.1 边框白平衡校正

由于每张图像边缘都包含大面积白色纸板，因此可以利用图像边框作为背景参考区域。首先提取图像四周边框像素，估计背景 RGB 中值；然后对 RGB 三通道施加轻量增益校正，使背景更接近中性白色。这样可以减弱不同图像之间可能存在的整体偏色，提高后续颜色比较的一致性。

### 3.2 棉花区域分割

图像经校正后，被转换到 CIELAB 色彩空间。设边框背景的 Lab 中值为 `Lab_bg`，对每个像素计算其与背景的差异：

- 与背景的欧氏色差；
- L* 亮度是否明显低于背景；
- 色度是否显著高于背景。

通过联合阈值构建初始前景掩膜，然后使用形态学开运算、闭运算和孔洞填充，去除噪点并保留主体棉花区域。若分割失败，则启用以图像中心为核心的后备分割策略。

### 3.3 稳健主色提取

棉花虽然属于同一种颜色，但由于其表面存在纤维纹理、褶皱和局部阴影，因此不能直接对全部前景像素简单求平均。为增强鲁棒性，本文采用如下稳健估计策略：

1. 在 `a*b*` 平面上以中位数为中心，剔除远离主色簇的离群点；
2. 在 `L*` 维度上做分位数截断，去除过暗阴影和过亮高光；
3. 对保留像素做均值估计，得到单张图像的主颜色 `Lab`。

这样既保留了颜色主体，又减少了背景残留和阴影对结果的影响。

### 3.4 多图原型稳定化

同一颜色组的多张图像在语义上应对应同一种棉花颜色，因此本文进一步引入组级颜色原型。具体做法是：

- 从同组 6 张训练图像中提取单图主色；
- 用最小总 ΔE00 的 medoid 作为该组的颜色原型 `prototype_lab`；
- 若某张图像的单图结果与原型差异过大，则将其结果向原型适度收缩。

该步骤相当于利用“同组先验”抑制异常样本，从而显著提升多图一致性。

### 3.5 标准颜色匹配

对于最终的 `dominant_lab`，与颜色数据库中所有 `lab_D65` 计算 CIEDE2000 色差 `ΔE00`，按距离从小到大排序，取前 3 名作为 Top-3 匹配结果。

---

## 4. 关键评价指标

本实验主要考察以下三类指标：

1. **主色提取误差**：预测主色与参考真值之间的 `ΔE00`；
2. **Top-3 匹配准确率**：数据库匹配的前 2 名是否正确；
3. **多图一致性**：同组 6 张图像主色两两间最大 `ΔE00`。

其中，多图一致性定义为：

> 同组全部图像主色两两比较后，取最大的 `ΔE00` 作为该组 `consistency_score`。

该值越小，说明算法对蓬松度、面积和局部阴影变化越不敏感，系统稳定性越好。

---

## 5. 实现细节

实验代码主要由以下几个文件组成：

- `color_utils.py`：实现 RGB/Lab 转换、ΔE00、数据库匹配等基础函数；
- `robust_cotton_color.py`：实现主算法流程；
- `main.py`：命令行入口，可选择 `train / test / all` 三种处理模式；
- `generate_report.py`：根据 JSON 结果自动生成 Markdown 报告。

程序支持按照题目给定的思路，使用每组 10 张图中的 6 张作为“原型建立集”，另外 4 张作为“测试集”；也支持直接处理全部 10 张图像。

---

## 6. 实验步骤

1. 准备数据目录 `cotton_image/` 和颜色数据库 `color_dataset.json`；
2. 运行主程序，提取训练集或测试集结果；
3. 生成每组 JSON 结果和汇总结果；
4. 统计各组一致性分数；
5. 使用 `generate_report.py` 自动生成实验报告初稿；
6. 在报告中补充最终实验现象与结论。

建议命令如下：

```bash
python main.py --data-root ./cotton_image --color-db ./color_dataset.json --output-dir ./outputs --subset train
python main.py --data-root ./cotton_image --color-db ./color_dataset.json --output-dir ./outputs --subset test
python generate_report.py --results ./outputs/results_test.json --output ./实验报告_棉花颜色识别.md
```

---

## 7. 实验结果（待本地实测后填入）

### 7.1 汇总指标

| 指标 | 实测结果 |
|---|---:|
| 平均一致性（raw） | 待填写 |
| 平均一致性（final） | 待填写 |
| 最大一致性 | 待填写 |
| 满足 consistency ≤ 2.5 的颜色组数 | 待填写 |

### 7.2 单组结果示例

| 颜色组 | 原型 Lab | 一致性分数 | Top-1 匹配 | Top-2 匹配 | Top-3 匹配 |
|---|---|---:|---|---|---|
| gray/1 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 |
| gray/2 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 |
| colorful/7 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 |

---

## 8. 实验分析

预计该算法在以下方面具备优势：

1. **背景固定条件下分割可靠**：白色纸板使背景建模简单，前景提取更稳定；
2. **Lab 空间更符合人眼感知**：与 RGB 相比，Lab 更适合做颜色距离度量；
3. **多图原型稳定化有效抑制离群样本**：能够降低棉花疏密变化与阴影影响；
4. **结果可解释性强**：每一步均有明确颜色学与图像处理依据。

同时，本方法也存在一定局限：

- 若某些图像边框被棉花遮挡较多，背景估计可能受影响；
- 对非常浅色或接近背景色的棉花，分割阈值需要更谨慎；
- 如果后续采集环境发生变化，则需要重新调整白平衡与分割阈值。

---

## 9. 结论

本文针对未漂白原棉图像的主颜色识别任务，提出了一套基于颜色科学和稳健图像处理的自动识别系统。该系统利用白色背景进行校正与分割，在 Lab 空间中稳健提取主色，并通过组级颜色原型提升多图一致性，最终以 ΔE00 为准则完成与标准颜色库的匹配。

该方法不依赖复杂训练，具有实现简单、解释性强、运行稳定的特点，适合本课题的数据特点与评分要求。若后续进一步结合更精细的分割与自适应阈值策略，系统性能仍有提升空间。
